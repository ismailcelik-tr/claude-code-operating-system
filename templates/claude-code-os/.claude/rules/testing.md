# Testing Rules

- Add or update tests when behavior changes.
- Prefer focused tests near the changed code.
- Run the smallest meaningful verification first.
- For shared logic, run broader checks before closing the task.
- If tests cannot be run, explain why and record the residual risk in `SESSION_CONTEXT.md`.
